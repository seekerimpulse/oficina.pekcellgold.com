create table user_red_temporal(
id int auto_increment primary key,
id_user varchar(10),
id_red_temporal varchar(10),
id_red_a_red varchar(10)
);


create table user_soporte(
id int auto_increment primary key,
id_user varchar(10),
id_red_temporal varchar (10)
);

